React + Material-UI + Ripout workflow simulation assembled as a Spring-Boot runnable JAR.

To run:
1) Download the jar. 
2) At command line: java -jar .\ripout-0.0.1-SNAPSHOT.jar

Open browser to http://localhost:8080
